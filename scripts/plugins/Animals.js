/* @flow */
'use strict';

/**
 * Plugin for animal population simulation.
 */
CivClicker.plugins.Animals = (() => {

  let wolfes = 0;
  let bunnies = 0;
  
  return new (class AnimalsPlugin {

    constructor() {
    }

    init() {
    }

    /**
     * @param {number} amount
     */
    killWolf(amount) {
    }

    /**
     * @param {number} amount
     */
    killBunny(amount) {
    }
  });
})();
